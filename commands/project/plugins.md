---
description: Discover, evaluate, and recommend tools across 7 categories for this project
allowed-tools: Read, Write, Edit, Glob, Grep, Bash(npm:*), Bash(pip:*), Bash(gh:*), Bash(git:*), Bash(curl:*), WebFetch, WebSearch
---

Run the project-plugins skill to discover and recommend tools for this project.

If $ARGUMENTS is not empty, use it to narrow the search scope (e.g., "database tools", "testing", "MCP servers for AWS").

Follow the discovery process exactly: understand needs, check curated registry first, search web for gaps, evaluate, present tiered recommendations with context cost summary, then ask which to install.
